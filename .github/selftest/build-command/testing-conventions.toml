# Native-binding self-test fixture: `widget.py` imports `generated`, produced by the
# `build_command` the suite-executing jobs run before pytest — the stand-in for a maturin/napi
# native module. `branch = false` because the fixture exercises the build hook, not branches.
[python]
coverage = { branch = false, fail_under = 100 }
