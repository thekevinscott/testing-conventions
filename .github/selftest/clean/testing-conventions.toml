# Clean self-test fixture: classify() is fully exercised, so the unit suite clears the floor
# and the coverage, colocated-test, and integration-lint jobs all pass.
[python]
coverage = { branch = true, fail_under = 100 }
