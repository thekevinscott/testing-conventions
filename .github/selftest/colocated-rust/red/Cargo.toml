# Red self-test crate `orphan`: `src/lib.rs` defines a bodied function with no inline
# `#[cfg(test)]` module, which the rust colocated-test presence arm must flag.
# `[workspace]` keeps it standalone.
[package]
name = "orphan"
version = "0.0.0"
edition = "2021"

[workspace]
