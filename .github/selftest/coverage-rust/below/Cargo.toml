# Below-floor fixture crate for the Rust `unit coverage` arm: the inline test exercises one
# arm of a branch, so `cargo llvm-cov` reports under 100% and the crate fails a 100 floor.
# `[workspace]` isolates the fixture from the parent cargo workspace.
[package]
name = "cov_rust_selftest_below"
version = "0.0.0"
edition = "2021"

[workspace]
