# Clean fixture crate for the Rust `unit coverage` arm: `add` is fully exercised by its
# colocated inline test, so `cargo llvm-cov` reports 100% and the crate clears a 100 floor.
# `[workspace]` isolates the fixture from the parent cargo workspace.
[package]
name = "cov_rust_selftest_clean"
version = "0.0.0"
edition = "2021"

[workspace]
