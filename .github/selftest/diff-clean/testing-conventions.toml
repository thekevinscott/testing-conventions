# Diff-scoped self-test fixture: classify() is fully exercised, so its changed lines clear the
# floor under `coverage --base` and the added source carries the colocated test that
# `colocated-test --base` requires.
[python]
coverage = { branch = true, fail_under = 90 }
