# Clean fixture crate for the `unit lint` job: `detect` must recognize a crate and fan the
# isolation job over `rust`, and the rule must pass this well-isolated unit.
# `[workspace]` isolates it from cargo.
[package]
name = "iso_selftest_clean"
version = "0.0.0"
edition = "2021"

[workspace]
