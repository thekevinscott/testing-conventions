[project]
name = "monorepo-py-fixture"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["attrs>=23"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/widget"]
