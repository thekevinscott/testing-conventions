# Fixture-local config for the monorepo selftest: no root manifest, so every gate must be
# driven by the derived package root.

# Point.shift has no branches, and the tool treats a zero-branch measurement as misconfigured
# rather than a vacuous pass — so this package-root fixture opts out of branch coverage.
[python.coverage]
branch = false
