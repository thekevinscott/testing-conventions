# Selftest crate: its colocated test asserts the real result, so every mutation of `add` is
# caught and `unit mutation` passes. `[workspace]` keeps it standalone.
[package]
name = "selftest_mutation_clean"
version = "0.0.0"
edition = "2021"

[workspace]
