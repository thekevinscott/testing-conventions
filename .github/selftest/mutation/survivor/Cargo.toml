# Selftest crate: its colocated test executes `is_positive` but asserts nothing, so mutants
# survive un-exempted and `unit mutation` must fail. `[workspace]` keeps it standalone.
[package]
name = "selftest_mutation_survivor"
version = "0.0.0"
edition = "2021"

[workspace]
