# Fixture-local config for the packaging-package-root self-test (#280): this package carries
# no source, so only the packaging gate has anything to check — the calling job scopes to it
# via `gates`.
