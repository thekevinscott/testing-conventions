# The pytest plugin's adoption of `unit one-function-per-file`; every other floor stays at the
# zero-config default. 12 is the lowest threshold this tree passes today — walk it down as
# `config/detect.py` and `pytest_plugin/hooks.py` split.
[python]
one_function_per_file = { max_lines = 12 }
