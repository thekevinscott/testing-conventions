# Exemptions only — no coverage thresholds (both keys are optional).
[[python.exempt]]
path = "src/cli.py"
rules = ["colocated-test", "coverage"]
reason = "thin launcher; logic in run()"

[[typescript.exempt]]
path = "src/index.ts"
rules = ["colocated-test"]
reason = "pure re-export barrel"
