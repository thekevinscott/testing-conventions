# Waiver for the integration-isolation rule: exempt the file from
# `no-first-party-patch`, with a reason (auditable, reason-required).
[[python.exempt]]
path = "tests/integration/charge_test.py"
rules = ["no-first-party-patch"]
reason = "legacy integration test; the first-party ledger seam is being extracted separately"
