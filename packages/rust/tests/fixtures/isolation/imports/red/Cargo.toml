# Red fixture crate for the Rust unit-isolation rule's import detector (#44, D2).
# Parsed by `syn`, never compiled — `[workspace]` isolates it from cargo.
[package]
name = "iso_imports_red"
version = "0.0.0"
edition = "2021"

[dependencies]
rand = "0.8"

[dev-dependencies]
mockall = "0.13"

[workspace]
