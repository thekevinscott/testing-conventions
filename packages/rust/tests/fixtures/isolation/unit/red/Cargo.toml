# Red fixture crate for the Rust unit-isolation rule (#44). Parsed by `syn`, never
# compiled — `[workspace]` keeps cargo from absorbing it into the tool's build.
[package]
name = "iso_unit_red"
version = "0.0.0"
edition = "2021"

[dependencies]
rand = "0.8"

[dev-dependencies]
mockall = "0.13"

[workspace]
