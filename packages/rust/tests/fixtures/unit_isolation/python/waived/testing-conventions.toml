# Waiver for the unit-isolation rule: exempt the file from
# `unmocked-collaborator`, with a reason (auditable, reason-required).
[[python.exempt]]
path = "widget_test.py"
rules = ["unmocked-collaborator"]
reason = "legacy unit test reaching a real ledger; the isolation refactor is tracked separately"
