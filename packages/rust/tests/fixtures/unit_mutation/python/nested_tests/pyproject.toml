# The package root: pytest's own upward search anchors its rootdir here, so both the
# top-level colocated suite under src/ and the one nested in src/pkg/ resolve their imports
# against the scan path while the package-level tests/ tier stays out of the src-scoped run.
[tool.pytest.ini_options]
