# Fixture crate `mut_killed`: its colocated test asserts the real result, so every
# cargo-mutants mutation of `add` changes the output and is caught — no survivors.
# `[workspace]` keeps it standalone (not part of the repo's cargo workspace).
[package]
name = "mut_killed"
version = "0.0.0"
edition = "2021"

[workspace]
