[package]
name = "agent-context-py"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "agent_context"
crate-type = ["cdylib"]

[dependencies]
agent-context-core = { path = "../../core" }
pyo3 = { version = "0.29.0", features = ["extension-module", "abi3-py39"] }
