[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "agent-context"
version = "0.0.0"
requires-python = ">=3.9"

[project.scripts]
agent-context-py = "agent_context:main"

[tool.maturin]
bindings = "pyo3"
